interface Product {
  id: number;
  name: string;
  quantity: number;
  unitPrice: number;
  totalValue: number;
}

interface FormRegisteredProduct {
  name: string;
  quantity: number;
  unitPrice: number;
  totalValue: number;
}